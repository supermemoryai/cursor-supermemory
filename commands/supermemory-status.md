---
name: supermemory-status
description: Show Supermemory authentication status
---

Run:

```bash
node "${CURSOR_PLUGIN_ROOT}/dist/cli.js" status
```

If it fails, rebuild or reinstall the plugin, then reload the window.
